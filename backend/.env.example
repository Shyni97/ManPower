# Environment Configuration
# Copy this file to .env and update the values

# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Configuration
# Use the provided MongoDB Atlas URL or your own
MONGO_URI=mongodb+srv://it23566552_db_user:GayalShyni%40%232097@manpowercluster.svpizzx.mongodb.net/ManPowerCluster?retryWrites=true&w=majority


# JWT Secret Key
# Generate a strong random secret key for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Client URL (Frontend)
# Update this to your frontend URL in production
CLIENT_URL=http://localhost:3000
